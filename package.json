{
	"name": "@kattebak/typespec-graphql-sdk-emitter",
	"version": "0.0.0",
	"description": "TypeSpec emitter that generates a typed GraphQL SDK from a TypeSpec source",
	"type": "module",
	"main": "dist/index.js",
	"publishConfig": {
		"access": "public"
	},
	"files": [
		"dist",
		"tsp"
	],
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"default": "./dist/index.js",
			"typespec": "./tsp/main.tsp"
		}
	},
	"scripts": {
		"fix": "npx @biomejs/biome check --write",
		"lint": "npx @biomejs/biome check",
		"build": "tsc",
		"watch": "tsc --watch",
		"test": "npm run build && npm run lint && npm run test:unit",
		"test:unit": "node --test --import=tsx src/**/*.test.ts",
		"prepublishOnly": "tsc"
	},
	"dependencies": {
		"@typespec/compiler": "^1.4.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.7",
		"@types/node": "^22.18.11",
		"tsx": "^4.20.5",
		"typescript": "^5.9.3"
	}
}
